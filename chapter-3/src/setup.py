from setuptools import setup, find_packages

setup(
    name='hld-sagemaker-demo',
    version='1.0',
    description='SageMaker Example with HLD data.',
    author='xhagrg',
    author_email='ig0004@uah.edu',
    url='',
    packages=find_packages(exclude=('tests', 'docs'))
)
